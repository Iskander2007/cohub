# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Django
*.log
db.sqlite3
# Эфемерные файлы SQLite (WAL/SHM) — не версионируем.
db.sqlite3-shm
db.sqlite3-wal
/media
/staticfiles

# Runtime-логи приложения (JSON-логи платежей/ошибок/запросов).
logs/

# Артефакты нагрузочного тестирования (генерируются locust'ом).
loadtest_report.html
loadtest_stats_*.csv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.env.local

# Секреты алертов (коммитим только *.example).
monitoring/alertmanager/secrets/telegram_bot_token
monitoring/alertmanager/secrets/smtp_password

# Локальные бэкапы БД (создаются backup.sh / backup_data).
/backups
# Отчёты покрытия тестами (pytest-cov).
htmlcov/
.coverage
.coverage.*
.pytest_cache/

# OS
Thumbs.db
